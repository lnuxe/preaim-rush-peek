# PRD — PREAIM Rush Peek（Rush 抗压 / 快速转火补枪训练）

> 项目：无畏契约（Valorant）风格瞄准训练器
> 模块：PREAIM 系列场景
> 版本：v1.0
> 状态：待评审
> 作者：lnuxe

---

## 1. 一句话定位

**击杀一个目标后，立刻在对侧刷新下一个目标，训练玩家在 rush 压力下的快速转火（target switching）与补枪能力。**

对标无畏契约中「敌方多人 rush 进点 / 残局 1vN」的真实压力场景，让玩家在几何房间内反复练习「击杀 → 准星瞬间甩到对侧 → 再击杀」的肌肉记忆。

---

## 2. 背景与问题

### 2.1 社区诉求来源
- 评论关键词：**rush 抗压、快速补枪、转火、一人打死后左右随机刷第二个 bot**
- 典型痛点：真实对局里敌方 rush 时，玩家「杀完一个 → 找第二个目标 → 拉枪」的链条太长，导致被补枪反杀

### 2.2 要解决的问题
| 痛点 | 训练目标 |
|------|----------|
| 击杀后准星停留在原位，来不及转火 | 强制对侧刷新，逼迫大幅甩枪 |
| 不知道下个敌人出现在哪 | 左右位置交替/随机，训练预判 |
| 多目标压力下手忙脚乱 | 单目标 + 即时刷新，聚焦「连续击杀节奏」 |

### 2.3 非目标（明确不做）
- ❌ 不做多人同时在场（那是「多目标静态」场景）
- ❌ 不做真实地图 / 真实枪械模型（沿用纯几何房间 + 圆形目标）
- ❌ 不做任何对局注入 / 自瞄（见「红线」章节）

---

## 3. 用户与场景

### 3.1 目标用户
- **进阶 FPS 玩家**：无畏契约 / CS 中段位玩家，希望提升残局 1vN 转火能力
- **已有瞄准训练习惯的用户**：熟悉 KovaaK / Aim Lab 的 tile frenzy 类玩法，需要更有「对局感」的压力训练

### 3.2 使用场景
1. 排位前热身：快速 60 秒，把手感和转火节奏拉起来
2. 针对性短板训练：发现自己「补枪慢」，专项刷 Rush Peek
3. 直播/录屏传播：连续击杀 + 甩枪的视觉爽感，适合内容创作

---

## 4. 核心玩法规则

### 4.1 基础循环（核心 loop）
```
场上只有 1 个目标（激活态）
   ↓ 玩家击杀
立即在对侧区域刷新 1 个新目标
   ↓ 玩家甩枪 + 击杀
立即再刷新……（循环至时长结束）
```

### 4.2 关键规则
| 规则 | 定义 |
|------|------|
| **目标数量** | 场上同时只存在 1 个可击杀目标（`numTargets: 1`） |
| **刷新时机** | 击杀即刷新（`removeClosestOnMiss` 保证击杀即移除） |
| **刷新位置** | 在「对侧」区域刷新（左右交替，`explicitOrder` 控制顺序） |
| **击杀判定** | 单击击杀（`clickSingle`） |
| **失误处理** | 打空时移除最近目标（`removeClosestOnMiss`），触发惩罚性重新刷新 |
| **时长** | 单局 60 秒 |

### 4.3 难度设计
- 通过「左右距离」控制甩枪幅度：`Rush Peek`（窄，±42%）→ `Rush Peek Wide`（宽，±45%）
- 目标半径随等级动态缩放（`targetRadiusMultiplier`），越高越小越难

---

## 5. 场景变体（首批交付）

| 场景名 | 定位 | 关键差异 |
|--------|------|----------|
| **Rush Peek** | 标准 rush 抗压 | 房间宽 240，左右区域 ±42%，目标半径 2.4 |
| **Rush Peek Wide** | 大角度转火 | 房间宽 300，左右区域 ±45%，目标半径 2.2（更难） |

> 后续可扩展：`Rush Peek Random`（左右随机而非固定交替）、`Rush Peek Tri`（上/左/右三区）。

---

## 6. 技术落地约束（数据驱动，零 C++ 改动）

复用现有引擎能力，场景通过 JSON 描述，不新增 C++ 代码：

| 引擎能力 | 在本场景的用法 |
|----------|----------------|
| `TargetRegion` 矩形区域 | 定义「左 / 右」两个对侧刷新区 |
| `explicitOrder` | 控制左右严格交替刷新顺序（右→左→右→左） |
| `newestTargetIsGhost` | 可选：新目标先以 ghost 态显示，给玩家预瞄提示 |
| `numTargets: 1` | 保证场上只有一个可击杀目标 |
| `clickSingle` + `removeClosestOnMiss` | 单击击杀 + 打空惩罚 |

### 6.1 数据字段映射（示例）
```json
{
  "name": "Rush Peek",
  "def": {
    "durationSeconds": 60,
    "shotType": { "clickSingle": true, "removeClosestOnMiss": true },
    "targetDef": {
      "numTargets": 1,
      "profiles": [{ "targetRadius": 2.4 }],
      "newestTargetIsGhost": true
    },
    "staticDef": {
      "targetPlacementStrategy": {
        "regions": [ /* Right(0.42) */, /* Left(-0.42) */ ],
        "regionsInfo": { "explicitOrder": [0, 1] },
        "minDistance": { "value": 20 }
      }
    }
  }
}
```

---

## 7. 验收标准（Definition of Done）

### 7.1 功能验收
- [ ] 场景加载后，场上同时最多只有 1 个可击杀目标
- [ ] 击杀目标后，新目标在**对侧**区域刷新（不刷新回同侧）
- [ ] 左右刷新严格交替（或符合 `explicitOrder` 定义）
- [ ] 打空（miss）时目标被移除并触发重新刷新
- [ ] 单局 60 秒自动结束，击杀数可统计

### 7.2 体验验收
- [ ] 60 秒内玩家能稳定完成「连续击杀 → 转火」循环，无明显卡顿/空窗
- [ ] 甩枪幅度符合难度设定（窄/宽两档可感知区分）
- [ ] 目标大小随等级缩放生效

### 7.3 质量验收
- [ ] 编译通过（无 C++ 改动，纯数据）
- [ ] 场景在列表可见、可选中、可正常进入/退出
- [ ] 中英文文案完整（本地化 key 补齐）

---

## 8. 红线（合规声明）

- 本项目是**离线单机瞄准训练器**，不注入任何对局进程
- 明确**拒绝**：自瞄、透视、锁头、对局内换皮等任何影响公平性的功能
- 参考社区反馈：作弊诉求一律不落地

---

## 9. 后续迭代方向（非本次范围）

1. **Rush Peek Random**：左右随机（而非固定交替），更贴近真实对局
2. **Rush Peek Tri / Quad**：三区、四区转火，提升压力上限
3. **击杀节奏统计**：记录「击杀间隔（KPS）」作为核心训练指标
4. **连杀反馈**：连续击杀的视觉/音效反馈，增强爽感

---

## 附：术语表

| 术语 | 说明 |
|------|------|
| Rush 抗压 | 模拟敌方多人快速进点，训练连续补枪 |
| 转火（Target Switching） | 击杀后准星快速切换到下一个目标 |
| Ghost 目标 | 半透明预览目标，提示下一目标位置 |
| 对侧刷新 | 新目标出现在上一个目标的相对位置 |