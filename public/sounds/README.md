# 音效（可选覆盖）

把真实 MP3 音效放到本目录，文件名固定如下：

| 文件 | 触发时机 |
|------|---------|
| `kill.mp3` | 普通击杀 |
| `headshot.mp3` | 爆头（命中目标中心） |
| `multikill.mp3` | 连杀 ≥2 |
| `miss.mp3` | 打空 |
| `end.mp3` | 单局结束 |

- 放入后自动预加载并优先播放（低延迟）。
- 缺省则回退 Web Audio 合成音效。

社区素材站见 `DEVELOPMENT.md` §4.2。
